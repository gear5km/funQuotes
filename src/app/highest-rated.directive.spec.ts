import { HighestRatedDirective } from './highest-rated.directive';

describe('HighestRatedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighestRatedDirective();
    expect(directive).toBeTruthy();
  });
});
